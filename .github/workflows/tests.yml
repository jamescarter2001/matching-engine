name: Tests (Java)
on:
  push:

jobs:
  tests-java:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v4
        with:
          java-version: 25
          distribution: temurin
          architecture: x64
          cache: maven
      - run: mvn test